/**
 * Auto-generated by scripts/build-icons.mjs — do not edit manually.
 * Run: npm run build-icons
 * Source: public/icons/*.svg
 * Registers the `reltio/data-quality` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/reltio/data-quality";
 * Optional component: import { ReltioDataQuality } from "@reltio/design/icons/reltio/data-quality";
 */
import { Icon, type IconProps } from "@reltio/design/components";
import { registerReltioIcon } from "../registerReltioIcon";

const ICON_NAME = registerReltioIcon("data-quality", {
	viewBox: "0 0 20 20",
	customTemplateAsString:
		'<g fill="none"><path d="M1.99998 17H6.33655V8.99998H1.99998V17ZM7.8365 17H12.1635V2.99998H7.8365V17ZM13.6634 17H18V11H13.6634V17ZM0.5 18.5V7.5H6.33655V1.5H13.6634V9.5H19.5V18.5H0.5Z" fill="currentColor"/></g>',
});

export type ReltioDataQualityProps = Omit<IconProps, "name">;

/** Renders `<Icon name="reltio/data-quality" />` after this module is evaluated. */
export function ReltioDataQuality(props: ReltioDataQualityProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`reltio/data-quality`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
