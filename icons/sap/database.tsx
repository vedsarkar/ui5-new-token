/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `database` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/database";
 * Optional component: import { Database } from "@reltio/design/icons/sap/database";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/database.js";

export type DatabaseProps = Omit<IconProps, "name">;

/** Renders `<Icon name="database" />` after this module is evaluated. */
export function Database(props: DatabaseProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`database`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
