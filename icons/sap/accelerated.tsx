/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `accelerated` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/accelerated";
 * Optional component: import { Accelerated } from "@reltio/design/icons/sap/accelerated";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/accelerated.js";

export type AcceleratedProps = Omit<IconProps, "name">;

/** Renders `<Icon name="accelerated" />` after this module is evaluated. */
export function Accelerated(props: AcceleratedProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`accelerated`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
