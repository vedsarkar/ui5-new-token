/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `navigation-down-arrow` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/navigation-down-arrow";
 * Optional component: import { NavigationDownArrow } from "@reltio/design/icons/sap/navigation-down-arrow";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/navigation-down-arrow.js";

export type NavigationDownArrowProps = Omit<IconProps, "name">;

/** Renders `<Icon name="navigation-down-arrow" />` after this module is evaluated. */
export function NavigationDownArrow(props: NavigationDownArrowProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`navigation-down-arrow`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
