/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `batch-payments` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/batch-payments";
 * Optional component: import { BatchPayments } from "@reltio/design/icons/sap/batch-payments";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/batch-payments.js";

export type BatchPaymentsProps = Omit<IconProps, "name">;

/** Renders `<Icon name="batch-payments" />` after this module is evaluated. */
export function BatchPayments(props: BatchPaymentsProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`batch-payments`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
