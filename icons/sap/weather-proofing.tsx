/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `weather-proofing` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/weather-proofing";
 * Optional component: import { WeatherProofing } from "@reltio/design/icons/sap/weather-proofing";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/weather-proofing.js";

export type WeatherProofingProps = Omit<IconProps, "name">;

/** Renders `<Icon name="weather-proofing" />` after this module is evaluated. */
export function WeatherProofing(props: WeatherProofingProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`weather-proofing`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
