/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `laptop` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/laptop";
 * Optional component: import { Laptop } from "@reltio/design/icons/sap/laptop";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/laptop.js";

export type LaptopProps = Omit<IconProps, "name">;

/** Renders `<Icon name="laptop" />` after this module is evaluated. */
export function Laptop(props: LaptopProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`laptop`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
