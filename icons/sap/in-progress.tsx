/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `in-progress` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/in-progress";
 * Optional component: import { InProgress } from "@reltio/design/icons/sap/in-progress";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/in-progress.js";

export type InProgressProps = Omit<IconProps, "name">;

/** Renders `<Icon name="in-progress" />` after this module is evaluated. */
export function InProgress(props: InProgressProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`in-progress`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
