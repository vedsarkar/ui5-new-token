/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `download` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/download";
 * Optional component: import { Download } from "@reltio/design/icons/sap/download";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/download.js";

export type DownloadProps = Omit<IconProps, "name">;

/** Renders `<Icon name="download" />` after this module is evaluated. */
export function Download(props: DownloadProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`download`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
