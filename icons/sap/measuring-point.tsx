/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `measuring-point` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/measuring-point";
 * Optional component: import { MeasuringPoint } from "@reltio/design/icons/sap/measuring-point";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/measuring-point.js";

export type MeasuringPointProps = Omit<IconProps, "name">;

/** Renders `<Icon name="measuring-point" />` after this module is evaluated. */
export function MeasuringPoint(props: MeasuringPointProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`measuring-point`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
