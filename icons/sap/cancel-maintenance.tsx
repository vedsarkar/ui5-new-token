/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `cancel-maintenance` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/cancel-maintenance";
 * Optional component: import { CancelMaintenance } from "@reltio/design/icons/sap/cancel-maintenance";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/cancel-maintenance.js";

export type CancelMaintenanceProps = Omit<IconProps, "name">;

/** Renders `<Icon name="cancel-maintenance" />` after this module is evaluated. */
export function CancelMaintenance(props: CancelMaintenanceProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`cancel-maintenance`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
