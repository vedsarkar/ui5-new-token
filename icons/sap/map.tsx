/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `map` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/map";
 * Optional component: import { MapIcon } from "@reltio/design/icons/sap/map";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/map.js";

export type MapIconProps = Omit<IconProps, "name">;

/** Renders `<Icon name="map" />` after this module is evaluated. */
export function MapIcon(props: MapIconProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`map`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
