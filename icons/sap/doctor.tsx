/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `doctor` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/doctor";
 * Optional component: import { Doctor } from "@reltio/design/icons/sap/doctor";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/doctor.js";

export type DoctorProps = Omit<IconProps, "name">;

/** Renders `<Icon name="doctor" />` after this module is evaluated. */
export function Doctor(props: DoctorProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`doctor`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
