/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `smart-watch` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/smart-watch";
 * Optional component: import { SmartWatch } from "@reltio/design/icons/sap/smart-watch";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/smart-watch.js";

export type SmartWatchProps = Omit<IconProps, "name">;

/** Renders `<Icon name="smart-watch" />` after this module is evaluated. */
export function SmartWatch(props: SmartWatchProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`smart-watch`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
