/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `currency` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/currency";
 * Optional component: import { Currency } from "@reltio/design/icons/sap/currency";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/currency.js";

export type CurrencyProps = Omit<IconProps, "name">;

/** Renders `<Icon name="currency" />` after this module is evaluated. */
export function Currency(props: CurrencyProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`currency`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
