/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `crm-sales` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/crm-sales";
 * Optional component: import { CrmSales } from "@reltio/design/icons/sap/crm-sales";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/crm-sales.js";

export type CrmSalesProps = Omit<IconProps, "name">;

/** Renders `<Icon name="crm-sales" />` after this module is evaluated. */
export function CrmSales(props: CrmSalesProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`crm-sales`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
