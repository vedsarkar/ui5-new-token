/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `direction-arrows` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/direction-arrows";
 * Optional component: import { DirectionArrows } from "@reltio/design/icons/sap/direction-arrows";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/direction-arrows.js";

export type DirectionArrowsProps = Omit<IconProps, "name">;

/** Renders `<Icon name="direction-arrows" />` after this module is evaluated. */
export function DirectionArrows(props: DirectionArrowsProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`direction-arrows`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
