/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `bar-chart` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/bar-chart";
 * Optional component: import { BarChart } from "@reltio/design/icons/sap/bar-chart";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/bar-chart.js";

export type BarChartProps = Omit<IconProps, "name">;

/** Renders `<Icon name="bar-chart" />` after this module is evaluated. */
export function BarChart(props: BarChartProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`bar-chart`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
