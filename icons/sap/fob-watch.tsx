/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `fob-watch` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/fob-watch";
 * Optional component: import { FobWatch } from "@reltio/design/icons/sap/fob-watch";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/fob-watch.js";

export type FobWatchProps = Omit<IconProps, "name">;

/** Renders `<Icon name="fob-watch" />` after this module is evaluated. */
export function FobWatch(props: FobWatchProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`fob-watch`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
