/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `eam-work-order` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/eam-work-order";
 * Optional component: import { EamWorkOrder } from "@reltio/design/icons/sap/eam-work-order";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/eam-work-order.js";

export type EamWorkOrderProps = Omit<IconProps, "name">;

/** Renders `<Icon name="eam-work-order" />` after this module is evaluated. */
export function EamWorkOrder(props: EamWorkOrderProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`eam-work-order`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
