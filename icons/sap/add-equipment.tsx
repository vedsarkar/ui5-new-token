/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `add-equipment` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/add-equipment";
 * Optional component: import { AddEquipment } from "@reltio/design/icons/sap/add-equipment";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/add-equipment.js";

export type AddEquipmentProps = Omit<IconProps, "name">;

/** Renders `<Icon name="add-equipment" />` after this module is evaluated. */
export function AddEquipment(props: AddEquipmentProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`add-equipment`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
