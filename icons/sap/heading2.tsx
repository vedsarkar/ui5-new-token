/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `heading2` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/heading2";
 * Optional component: import { Heading2 } from "@reltio/design/icons/sap/heading2";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/heading2.js";

export type Heading2Props = Omit<IconProps, "name">;

/** Renders `<Icon name="heading2" />` after this module is evaluated. */
export function Heading2(props: Heading2Props) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`heading2`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
