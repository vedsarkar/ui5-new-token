/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `course-program` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/course-program";
 * Optional component: import { CourseProgram } from "@reltio/design/icons/sap/course-program";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/course-program.js";

export type CourseProgramProps = Omit<IconProps, "name">;

/** Renders `<Icon name="course-program" />` after this module is evaluated. */
export function CourseProgram(props: CourseProgramProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`course-program`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
