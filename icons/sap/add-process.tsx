/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `add-process` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/add-process";
 * Optional component: import { AddProcess } from "@reltio/design/icons/sap/add-process";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/add-process.js";

export type AddProcessProps = Omit<IconProps, "name">;

/** Renders `<Icon name="add-process" />` after this module is evaluated. */
export function AddProcess(props: AddProcessProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`add-process`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
