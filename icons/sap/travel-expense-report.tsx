/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `travel-expense-report` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/travel-expense-report";
 * Optional component: import { TravelExpenseReport } from "@reltio/design/icons/sap/travel-expense-report";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/travel-expense-report.js";

export type TravelExpenseReportProps = Omit<IconProps, "name">;

/** Renders `<Icon name="travel-expense-report" />` after this module is evaluated. */
export function TravelExpenseReport(props: TravelExpenseReportProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`travel-expense-report`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
