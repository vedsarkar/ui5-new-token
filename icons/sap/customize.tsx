/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `customize` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/customize";
 * Optional component: import { Customize } from "@reltio/design/icons/sap/customize";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/customize.js";

export type CustomizeProps = Omit<IconProps, "name">;

/** Renders `<Icon name="customize" />` after this module is evaluated. */
export function Customize(props: CustomizeProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`customize`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
