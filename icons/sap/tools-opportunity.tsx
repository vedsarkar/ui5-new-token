/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `tools-opportunity` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/tools-opportunity";
 * Optional component: import { ToolsOpportunity } from "@reltio/design/icons/sap/tools-opportunity";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/tools-opportunity.js";

export type ToolsOpportunityProps = Omit<IconProps, "name">;

/** Renders `<Icon name="tools-opportunity" />` after this module is evaluated. */
export function ToolsOpportunity(props: ToolsOpportunityProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`tools-opportunity`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
