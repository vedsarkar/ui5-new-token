/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `ppt-attachment` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/ppt-attachment";
 * Optional component: import { PptAttachment } from "@reltio/design/icons/sap/ppt-attachment";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/ppt-attachment.js";

export type PptAttachmentProps = Omit<IconProps, "name">;

/** Renders `<Icon name="ppt-attachment" />` after this module is evaluated. */
export function PptAttachment(props: PptAttachmentProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`ppt-attachment`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
