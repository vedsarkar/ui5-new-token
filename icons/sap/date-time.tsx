/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `date-time` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/date-time";
 * Optional component: import { DateTime } from "@reltio/design/icons/sap/date-time";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/date-time.js";

export type DateTimeProps = Omit<IconProps, "name">;

/** Renders `<Icon name="date-time" />` after this module is evaluated. */
export function DateTime(props: DateTimeProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`date-time`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
