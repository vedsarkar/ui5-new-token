/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `electronic-medical-record` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/electronic-medical-record";
 * Optional component: import { ElectronicMedicalRecord } from "@reltio/design/icons/sap/electronic-medical-record";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/electronic-medical-record.js";

export type ElectronicMedicalRecordProps = Omit<IconProps, "name">;

/** Renders `<Icon name="electronic-medical-record" />` after this module is evaluated. */
export function ElectronicMedicalRecord(props: ElectronicMedicalRecordProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`electronic-medical-record`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
