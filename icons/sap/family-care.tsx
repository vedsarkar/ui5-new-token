/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `family-care` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/family-care";
 * Optional component: import { FamilyCare } from "@reltio/design/icons/sap/family-care";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/family-care.js";

export type FamilyCareProps = Omit<IconProps, "name">;

/** Renders `<Icon name="family-care" />` after this module is evaluated. */
export function FamilyCare(props: FamilyCareProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`family-care`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
