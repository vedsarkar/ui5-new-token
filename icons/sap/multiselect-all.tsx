/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `multiselect-all` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/multiselect-all";
 * Optional component: import { MultiselectAll } from "@reltio/design/icons/sap/multiselect-all";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/multiselect-all.js";

export type MultiselectAllProps = Omit<IconProps, "name">;

/** Renders `<Icon name="multiselect-all" />` after this module is evaluated. */
export function MultiselectAll(props: MultiselectAllProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`multiselect-all`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
