/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `underline-text` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/underline-text";
 * Optional component: import { UnderlineText } from "@reltio/design/icons/sap/underline-text";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/underline-text.js";

export type UnderlineTextProps = Omit<IconProps, "name">;

/** Renders `<Icon name="underline-text" />` after this module is evaluated. */
export function UnderlineText(props: UnderlineTextProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`underline-text`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
