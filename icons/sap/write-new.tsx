/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `write-new` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/write-new";
 * Optional component: import { WriteNew } from "@reltio/design/icons/sap/write-new";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/write-new.js";

export type WriteNewProps = Omit<IconProps, "name">;

/** Renders `<Icon name="write-new" />` after this module is evaluated. */
export function WriteNew(props: WriteNewProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`write-new`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
