/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `primary-key` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/primary-key";
 * Optional component: import { PrimaryKey } from "@reltio/design/icons/sap/primary-key";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/primary-key.js";

export type PrimaryKeyProps = Omit<IconProps, "name">;

/** Renders `<Icon name="primary-key" />` after this module is evaluated. */
export function PrimaryKey(props: PrimaryKeyProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`primary-key`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
