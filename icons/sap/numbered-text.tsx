/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `numbered-text` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/numbered-text";
 * Optional component: import { NumberedText } from "@reltio/design/icons/sap/numbered-text";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/numbered-text.js";

export type NumberedTextProps = Omit<IconProps, "name">;

/** Renders `<Icon name="numbered-text" />` after this module is evaluated. */
export function NumberedText(props: NumberedTextProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`numbered-text`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
