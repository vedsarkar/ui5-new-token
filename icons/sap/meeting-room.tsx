/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `meeting-room` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/meeting-room";
 * Optional component: import { MeetingRoom } from "@reltio/design/icons/sap/meeting-room";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/meeting-room.js";

export type MeetingRoomProps = Omit<IconProps, "name">;

/** Renders `<Icon name="meeting-room" />` after this module is evaluated. */
export function MeetingRoom(props: MeetingRoomProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`meeting-room`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
