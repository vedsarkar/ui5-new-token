/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `meal` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/meal";
 * Optional component: import { Meal } from "@reltio/design/icons/sap/meal";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/meal.js";

export type MealProps = Omit<IconProps, "name">;

/** Renders `<Icon name="meal" />` after this module is evaluated. */
export function Meal(props: MealProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`meal`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
