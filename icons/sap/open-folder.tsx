/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `open-folder` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/open-folder";
 * Optional component: import { OpenFolder } from "@reltio/design/icons/sap/open-folder";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/open-folder.js";

export type OpenFolderProps = Omit<IconProps, "name">;

/** Renders `<Icon name="open-folder" />` after this module is evaluated. */
export function OpenFolder(props: OpenFolderProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`open-folder`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
