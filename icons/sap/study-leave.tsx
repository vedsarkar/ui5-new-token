/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `study-leave` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/study-leave";
 * Optional component: import { StudyLeave } from "@reltio/design/icons/sap/study-leave";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/study-leave.js";

export type StudyLeaveProps = Omit<IconProps, "name">;

/** Renders `<Icon name="study-leave" />` after this module is evaluated. */
export function StudyLeave(props: StudyLeaveProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`study-leave`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
