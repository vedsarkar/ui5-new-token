/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `checklist-item` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/checklist-item";
 * Optional component: import { ChecklistItem } from "@reltio/design/icons/sap/checklist-item";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/checklist-item.js";

export type ChecklistItemProps = Omit<IconProps, "name">;

/** Renders `<Icon name="checklist-item" />` after this module is evaluated. */
export function ChecklistItem(props: ChecklistItemProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`checklist-item`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
