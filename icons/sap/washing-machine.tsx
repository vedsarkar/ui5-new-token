/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `washing-machine` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/washing-machine";
 * Optional component: import { WashingMachine } from "@reltio/design/icons/sap/washing-machine";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/washing-machine.js";

export type WashingMachineProps = Omit<IconProps, "name">;

/** Renders `<Icon name="washing-machine" />` after this module is evaluated. */
export function WashingMachine(props: WashingMachineProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`washing-machine`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
