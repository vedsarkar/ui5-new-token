/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `filter-analytics` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/filter-analytics";
 * Optional component: import { FilterAnalytics } from "@reltio/design/icons/sap/filter-analytics";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/filter-analytics.js";

export type FilterAnalyticsProps = Omit<IconProps, "name">;

/** Renders `<Icon name="filter-analytics" />` after this module is evaluated. */
export function FilterAnalytics(props: FilterAnalyticsProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`filter-analytics`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
