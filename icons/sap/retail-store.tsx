/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `retail-store` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/retail-store";
 * Optional component: import { RetailStore } from "@reltio/design/icons/sap/retail-store";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/retail-store.js";

export type RetailStoreProps = Omit<IconProps, "name">;

/** Renders `<Icon name="retail-store" />` after this module is evaluated. */
export function RetailStore(props: RetailStoreProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`retail-store`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
