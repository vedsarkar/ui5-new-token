/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `opportunities` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/opportunities";
 * Optional component: import { Opportunities } from "@reltio/design/icons/sap/opportunities";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/opportunities.js";

export type OpportunitiesProps = Omit<IconProps, "name">;

/** Renders `<Icon name="opportunities" />` after this module is evaluated. */
export function Opportunities(props: OpportunitiesProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`opportunities`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
