/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `ui-notifications` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/ui-notifications";
 * Optional component: import { UiNotifications } from "@reltio/design/icons/sap/ui-notifications";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/ui-notifications.js";

export type UiNotificationsProps = Omit<IconProps, "name">;

/** Renders `<Icon name="ui-notifications" />` after this module is evaluated. */
export function UiNotifications(props: UiNotificationsProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`ui-notifications`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
