/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `my-sales-order` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/my-sales-order";
 * Optional component: import { MySalesOrder } from "@reltio/design/icons/sap/my-sales-order";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/my-sales-order.js";

export type MySalesOrderProps = Omit<IconProps, "name">;

/** Renders `<Icon name="my-sales-order" />` after this module is evaluated. */
export function MySalesOrder(props: MySalesOrderProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`my-sales-order`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
