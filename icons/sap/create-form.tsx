/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `create-form` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/create-form";
 * Optional component: import { CreateForm } from "@reltio/design/icons/sap/create-form";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/create-form.js";

export type CreateFormProps = Omit<IconProps, "name">;

/** Renders `<Icon name="create-form" />` after this module is evaluated. */
export function CreateForm(props: CreateFormProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`create-form`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
