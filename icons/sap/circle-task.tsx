/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `circle-task` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/circle-task";
 * Optional component: import { CircleTask } from "@reltio/design/icons/sap/circle-task";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/circle-task.js";

export type CircleTaskProps = Omit<IconProps, "name">;

/** Renders `<Icon name="circle-task" />` after this module is evaluated. */
export function CircleTask(props: CircleTaskProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`circle-task`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
