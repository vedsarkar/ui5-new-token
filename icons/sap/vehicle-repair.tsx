/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `vehicle-repair` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/vehicle-repair";
 * Optional component: import { VehicleRepair } from "@reltio/design/icons/sap/vehicle-repair";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/vehicle-repair.js";

export type VehicleRepairProps = Omit<IconProps, "name">;

/** Renders `<Icon name="vehicle-repair" />` after this module is evaluated. */
export function VehicleRepair(props: VehicleRepairProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`vehicle-repair`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
