/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `sort-ascending` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/sort-ascending";
 * Optional component: import { SortAscending } from "@reltio/design/icons/sap/sort-ascending";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/sort-ascending.js";

export type SortAscendingProps = Omit<IconProps, "name">;

/** Renders `<Icon name="sort-ascending" />` after this module is evaluated. */
export function SortAscending(props: SortAscendingProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`sort-ascending`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
