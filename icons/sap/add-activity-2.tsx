/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `add-activity-2` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/add-activity-2";
 * Optional component: import { AddActivity2 } from "@reltio/design/icons/sap/add-activity-2";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/add-activity-2.js";

export type AddActivity2Props = Omit<IconProps, "name">;

/** Renders `<Icon name="add-activity-2" />` after this module is evaluated. */
export function AddActivity2(props: AddActivity2Props) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`add-activity-2`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
