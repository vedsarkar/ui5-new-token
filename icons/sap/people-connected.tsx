/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `people-connected` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/people-connected";
 * Optional component: import { PeopleConnected } from "@reltio/design/icons/sap/people-connected";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/people-connected.js";

export type PeopleConnectedProps = Omit<IconProps, "name">;

/** Renders `<Icon name="people-connected" />` after this module is evaluated. */
export function PeopleConnected(props: PeopleConnectedProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`people-connected`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
