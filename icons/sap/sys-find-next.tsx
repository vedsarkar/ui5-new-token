/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `sys-find-next` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/sys-find-next";
 * Optional component: import { SysFindNext } from "@reltio/design/icons/sap/sys-find-next";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/sys-find-next.js";

export type SysFindNextProps = Omit<IconProps, "name">;

/** Renders `<Icon name="sys-find-next" />` after this module is evaluated. */
export function SysFindNext(props: SysFindNextProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`sys-find-next`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
