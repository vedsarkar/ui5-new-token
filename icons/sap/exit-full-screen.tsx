/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `exit-full-screen` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/exit-full-screen";
 * Optional component: import { ExitFullScreen } from "@reltio/design/icons/sap/exit-full-screen";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/exit-full-screen.js";

export type ExitFullScreenProps = Omit<IconProps, "name">;

/** Renders `<Icon name="exit-full-screen" />` after this module is evaluated. */
export function ExitFullScreen(props: ExitFullScreenProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`exit-full-screen`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
