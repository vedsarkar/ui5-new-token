/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `drill-down` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/drill-down";
 * Optional component: import { DrillDown } from "@reltio/design/icons/sap/drill-down";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/drill-down.js";

export type DrillDownProps = Omit<IconProps, "name">;

/** Renders `<Icon name="drill-down" />` after this module is evaluated. */
export function DrillDown(props: DrillDownProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`drill-down`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
