/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `detail-view` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/detail-view";
 * Optional component: import { DetailView } from "@reltio/design/icons/sap/detail-view";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/detail-view.js";

export type DetailViewProps = Omit<IconProps, "name">;

/** Renders `<Icon name="detail-view" />` after this module is evaluated. */
export function DetailView(props: DetailViewProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`detail-view`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
