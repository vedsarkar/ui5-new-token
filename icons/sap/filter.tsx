/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `filter` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/filter";
 * Optional component: import { Filter } from "@reltio/design/icons/sap/filter";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/filter.js";

export type FilterProps = Omit<IconProps, "name">;

/** Renders `<Icon name="filter" />` after this module is evaluated. */
export function Filter(props: FilterProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`filter`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
