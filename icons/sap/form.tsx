/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `form` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/form";
 * Optional component: import { Form } from "@reltio/design/icons/sap/form";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/form.js";

export type FormProps = Omit<IconProps, "name">;

/** Renders `<Icon name="form" />` after this module is evaluated. */
export function Form(props: FormProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`form`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
