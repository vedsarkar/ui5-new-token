/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `sap-box` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/sap-box";
 * Optional component: import { SapBox } from "@reltio/design/icons/sap/sap-box";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/sap-box.js";

export type SapBoxProps = Omit<IconProps, "name">;

/** Renders `<Icon name="sap-box" />` after this module is evaluated. */
export function SapBox(props: SapBoxProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`sap-box`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
