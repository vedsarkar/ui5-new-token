/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `map-3` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/map-3";
 * Optional component: import { Map3 } from "@reltio/design/icons/sap/map-3";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/map-3.js";

export type Map3Props = Omit<IconProps, "name">;

/** Renders `<Icon name="map-3" />` after this module is evaluated. */
export function Map3(props: Map3Props) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`map-3`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
