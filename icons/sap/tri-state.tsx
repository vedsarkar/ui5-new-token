/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `tri-state` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/tri-state";
 * Optional component: import { TriState } from "@reltio/design/icons/sap/tri-state";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/tri-state.js";

export type TriStateProps = Omit<IconProps, "name">;

/** Renders `<Icon name="tri-state" />` after this module is evaluated. */
export function TriState(props: TriStateProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`tri-state`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
