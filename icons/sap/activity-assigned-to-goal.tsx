/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `activity-assigned-to-goal` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/activity-assigned-to-goal";
 * Optional component: import { ActivityAssignedToGoal } from "@reltio/design/icons/sap/activity-assigned-to-goal";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/activity-assigned-to-goal.js";

export type ActivityAssignedToGoalProps = Omit<IconProps, "name">;

/** Renders `<Icon name="activity-assigned-to-goal" />` after this module is evaluated. */
export function ActivityAssignedToGoal(props: ActivityAssignedToGoalProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`activity-assigned-to-goal`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
