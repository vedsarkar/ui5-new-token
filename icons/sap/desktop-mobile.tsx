/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `desktop-mobile` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/desktop-mobile";
 * Optional component: import { DesktopMobile } from "@reltio/design/icons/sap/desktop-mobile";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/desktop-mobile.js";

export type DesktopMobileProps = Omit<IconProps, "name">;

/** Renders `<Icon name="desktop-mobile" />` after this module is evaluated. */
export function DesktopMobile(props: DesktopMobileProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`desktop-mobile`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
