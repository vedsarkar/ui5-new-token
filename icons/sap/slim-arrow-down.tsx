/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `slim-arrow-down` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/slim-arrow-down";
 * Optional component: import { SlimArrowDown } from "@reltio/design/icons/sap/slim-arrow-down";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/slim-arrow-down.js";

export type SlimArrowDownProps = Omit<IconProps, "name">;

/** Renders `<Icon name="slim-arrow-down" />` after this module is evaluated. */
export function SlimArrowDown(props: SlimArrowDownProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`slim-arrow-down`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
