/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `message-popup` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/message-popup";
 * Optional component: import { MessagePopup } from "@reltio/design/icons/sap/message-popup";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/message-popup.js";

export type MessagePopupProps = Omit<IconProps, "name">;

/** Renders `<Icon name="message-popup" />` after this module is evaluated. */
export function MessagePopup(props: MessagePopupProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`message-popup`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
