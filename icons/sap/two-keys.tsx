/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `two-keys` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/two-keys";
 * Optional component: import { TwoKeys } from "@reltio/design/icons/sap/two-keys";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/two-keys.js";

export type TwoKeysProps = Omit<IconProps, "name">;

/** Renders `<Icon name="two-keys" />` after this module is evaluated. */
export function TwoKeys(props: TwoKeysProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`two-keys`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
