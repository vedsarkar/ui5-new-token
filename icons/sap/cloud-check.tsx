/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `cloud-check` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/cloud-check";
 * Optional component: import { CloudCheck } from "@reltio/design/icons/sap/cloud-check";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/cloud-check.js";

export type CloudCheckProps = Omit<IconProps, "name">;

/** Renders `<Icon name="cloud-check" />` after this module is evaluated. */
export function CloudCheck(props: CloudCheckProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`cloud-check`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
