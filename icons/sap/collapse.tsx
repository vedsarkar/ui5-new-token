/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `collapse` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/collapse";
 * Optional component: import { Collapse } from "@reltio/design/icons/sap/collapse";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/collapse.js";

export type CollapseProps = Omit<IconProps, "name">;

/** Renders `<Icon name="collapse" />` after this module is evaluated. */
export function Collapse(props: CollapseProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`collapse`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
