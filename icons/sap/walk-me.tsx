/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `walk-me` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/walk-me";
 * Optional component: import { WalkMe } from "@reltio/design/icons/sap/walk-me";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/walk-me.js";

export type WalkMeProps = Omit<IconProps, "name">;

/** Renders `<Icon name="walk-me" />` after this module is evaluated. */
export function WalkMe(props: WalkMeProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`walk-me`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
