/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `internet-browser` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/internet-browser";
 * Optional component: import { InternetBrowser } from "@reltio/design/icons/sap/internet-browser";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/internet-browser.js";

export type InternetBrowserProps = Omit<IconProps, "name">;

/** Renders `<Icon name="internet-browser" />` after this module is evaluated. */
export function InternetBrowser(props: InternetBrowserProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`internet-browser`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
