/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `arrow-right` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/arrow-right";
 * Optional component: import { ArrowRight } from "@reltio/design/icons/sap/arrow-right";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/arrow-right.js";

export type ArrowRightProps = Omit<IconProps, "name">;

/** Renders `<Icon name="arrow-right" />` after this module is evaluated. */
export function ArrowRight(props: ArrowRightProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`arrow-right`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
