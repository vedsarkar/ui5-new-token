/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `line-chart-dual-axis` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/line-chart-dual-axis";
 * Optional component: import { LineChartDualAxis } from "@reltio/design/icons/sap/line-chart-dual-axis";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/line-chart-dual-axis.js";

export type LineChartDualAxisProps = Omit<IconProps, "name">;

/** Renders `<Icon name="line-chart-dual-axis" />` after this module is evaluated. */
export function LineChartDualAxis(props: LineChartDualAxisProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`line-chart-dual-axis`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
