/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `detail-more` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/detail-more";
 * Optional component: import { DetailMore } from "@reltio/design/icons/sap/detail-more";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/detail-more.js";

export type DetailMoreProps = Omit<IconProps, "name">;

/** Renders `<Icon name="detail-more" />` after this module is evaluated. */
export function DetailMore(props: DetailMoreProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`detail-more`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
