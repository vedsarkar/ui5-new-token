/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `arrow-left` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/arrow-left";
 * Optional component: import { ArrowLeft } from "@reltio/design/icons/sap/arrow-left";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/arrow-left.js";

export type ArrowLeftProps = Omit<IconProps, "name">;

/** Renders `<Icon name="arrow-left" />` after this module is evaluated. */
export function ArrowLeft(props: ArrowLeftProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`arrow-left`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
