/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `bold-text` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/bold-text";
 * Optional component: import { BoldText } from "@reltio/design/icons/sap/bold-text";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/bold-text.js";

export type BoldTextProps = Omit<IconProps, "name">;

/** Renders `<Icon name="bold-text" />` after this module is evaluated. */
export function BoldText(props: BoldTextProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`bold-text`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
