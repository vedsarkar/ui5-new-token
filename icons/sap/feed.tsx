/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `feed` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/feed";
 * Optional component: import { Feed } from "@reltio/design/icons/sap/feed";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/feed.js";

export type FeedProps = Omit<IconProps, "name">;

/** Renders `<Icon name="feed" />` after this module is evaluated. */
export function Feed(props: FeedProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`feed`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
