/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `sort-descending` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/sort-descending";
 * Optional component: import { SortDescending } from "@reltio/design/icons/sap/sort-descending";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/sort-descending.js";

export type SortDescendingProps = Omit<IconProps, "name">;

/** Renders `<Icon name="sort-descending" />` after this module is evaluated. */
export function SortDescending(props: SortDescendingProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`sort-descending`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
