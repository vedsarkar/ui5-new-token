/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `pdf-attachment` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/pdf-attachment";
 * Optional component: import { PdfAttachment } from "@reltio/design/icons/sap/pdf-attachment";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/pdf-attachment.js";

export type PdfAttachmentProps = Omit<IconProps, "name">;

/** Renders `<Icon name="pdf-attachment" />` after this module is evaluated. */
export function PdfAttachment(props: PdfAttachmentProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`pdf-attachment`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
