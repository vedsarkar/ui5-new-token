/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `example` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/example";
 * Optional component: import { Example } from "@reltio/design/icons/sap/example";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/example.js";

export type ExampleProps = Omit<IconProps, "name">;

/** Renders `<Icon name="example" />` after this module is evaluated. */
export function Example(props: ExampleProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`example`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
