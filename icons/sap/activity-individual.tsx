/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `activity-individual` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/activity-individual";
 * Optional component: import { ActivityIndividual } from "@reltio/design/icons/sap/activity-individual";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/activity-individual.js";

export type ActivityIndividualProps = Omit<IconProps, "name">;

/** Renders `<Icon name="activity-individual" />` after this module is evaluated. */
export function ActivityIndividual(props: ActivityIndividualProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`activity-individual`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
