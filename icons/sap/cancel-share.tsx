/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `cancel-share` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/cancel-share";
 * Optional component: import { CancelShare } from "@reltio/design/icons/sap/cancel-share";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/cancel-share.js";

export type CancelShareProps = Omit<IconProps, "name">;

/** Renders `<Icon name="cancel-share" />` after this module is evaluated. */
export function CancelShare(props: CancelShareProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`cancel-share`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
