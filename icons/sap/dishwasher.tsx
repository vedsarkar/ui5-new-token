/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `dishwasher` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/dishwasher";
 * Optional component: import { Dishwasher } from "@reltio/design/icons/sap/dishwasher";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/dishwasher.js";

export type DishwasherProps = Omit<IconProps, "name">;

/** Renders `<Icon name="dishwasher" />` after this module is evaluated. */
export function Dishwasher(props: DishwasherProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`dishwasher`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
