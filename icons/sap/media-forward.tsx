/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `media-forward` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/media-forward";
 * Optional component: import { MediaForward } from "@reltio/design/icons/sap/media-forward";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/media-forward.js";

export type MediaForwardProps = Omit<IconProps, "name">;

/** Renders `<Icon name="media-forward" />` after this module is evaluated. */
export function MediaForward(props: MediaForwardProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`media-forward`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
