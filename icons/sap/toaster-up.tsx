/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `toaster-up` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/toaster-up";
 * Optional component: import { ToasterUp } from "@reltio/design/icons/sap/toaster-up";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/toaster-up.js";

export type ToasterUpProps = Omit<IconProps, "name">;

/** Renders `<Icon name="toaster-up" />` after this module is evaluated. */
export function ToasterUp(props: ToasterUpProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`toaster-up`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
