/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `chart-table-view` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/chart-table-view";
 * Optional component: import { ChartTableView } from "@reltio/design/icons/sap/chart-table-view";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/chart-table-view.js";

export type ChartTableViewProps = Omit<IconProps, "name">;

/** Renders `<Icon name="chart-table-view" />` after this module is evaluated. */
export function ChartTableView(props: ChartTableViewProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`chart-table-view`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
