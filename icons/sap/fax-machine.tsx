/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `fax-machine` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/fax-machine";
 * Optional component: import { FaxMachine } from "@reltio/design/icons/sap/fax-machine";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/fax-machine.js";

export type FaxMachineProps = Omit<IconProps, "name">;

/** Renders `<Icon name="fax-machine" />` after this module is evaluated. */
export function FaxMachine(props: FaxMachineProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`fax-machine`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
