/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `bbyd-dashboard` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/bbyd-dashboard";
 * Optional component: import { BbydDashboard } from "@reltio/design/icons/sap/bbyd-dashboard";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/bbyd-dashboard.js";

export type BbydDashboardProps = Omit<IconProps, "name">;

/** Renders `<Icon name="bbyd-dashboard" />` after this module is evaluated. */
export function BbydDashboard(props: BbydDashboardProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`bbyd-dashboard`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
