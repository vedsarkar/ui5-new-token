/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `pdf-reader` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/pdf-reader";
 * Optional component: import { PdfReader } from "@reltio/design/icons/sap/pdf-reader";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/pdf-reader.js";

export type PdfReaderProps = Omit<IconProps, "name">;

/** Renders `<Icon name="pdf-reader" />` after this module is evaluated. */
export function PdfReader(props: PdfReaderProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`pdf-reader`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
