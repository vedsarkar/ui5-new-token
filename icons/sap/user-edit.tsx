/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `user-edit` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/user-edit";
 * Optional component: import { UserEdit } from "@reltio/design/icons/sap/user-edit";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/user-edit.js";

export type UserEditProps = Omit<IconProps, "name">;

/** Renders `<Icon name="user-edit" />` after this module is evaluated. */
export function UserEdit(props: UserEditProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`user-edit`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
