/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `clinical-task-tracker` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/clinical-task-tracker";
 * Optional component: import { ClinicalTaskTracker } from "@reltio/design/icons/sap/clinical-task-tracker";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/clinical-task-tracker.js";

export type ClinicalTaskTrackerProps = Omit<IconProps, "name">;

/** Renders `<Icon name="clinical-task-tracker" />` after this module is evaluated. */
export function ClinicalTaskTracker(props: ClinicalTaskTrackerProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`clinical-task-tracker`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
