/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `collections-management` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/collections-management";
 * Optional component: import { CollectionsManagement } from "@reltio/design/icons/sap/collections-management";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/collections-management.js";

export type CollectionsManagementProps = Omit<IconProps, "name">;

/** Renders `<Icon name="collections-management" />` after this module is evaluated. */
export function CollectionsManagement(props: CollectionsManagementProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`collections-management`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
