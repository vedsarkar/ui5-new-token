/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `workflow-tasks` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/workflow-tasks";
 * Optional component: import { WorkflowTasks } from "@reltio/design/icons/sap/workflow-tasks";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/workflow-tasks.js";

export type WorkflowTasksProps = Omit<IconProps, "name">;

/** Renders `<Icon name="workflow-tasks" />` after this module is evaluated. */
export function WorkflowTasks(props: WorkflowTasksProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`workflow-tasks`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
