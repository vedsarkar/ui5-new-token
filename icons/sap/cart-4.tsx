/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `cart-4` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/cart-4";
 * Optional component: import { Cart4 } from "@reltio/design/icons/sap/cart-4";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/cart-4.js";

export type Cart4Props = Omit<IconProps, "name">;

/** Renders `<Icon name="cart-4" />` after this module is evaluated. */
export function Cart4(props: Cart4Props) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`cart-4`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
