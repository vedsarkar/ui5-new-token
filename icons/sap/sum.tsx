/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `sum` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/sum";
 * Optional component: import { Sum } from "@reltio/design/icons/sap/sum";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/sum.js";

export type SumProps = Omit<IconProps, "name">;

/** Renders `<Icon name="sum" />` after this module is evaluated. */
export function Sum(props: SumProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`sum`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
