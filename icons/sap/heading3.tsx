/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `heading3` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/heading3";
 * Optional component: import { Heading3 } from "@reltio/design/icons/sap/heading3";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/heading3.js";

export type Heading3Props = Omit<IconProps, "name">;

/** Renders `<Icon name="heading3" />` after this module is evaluated. */
export function Heading3(props: Heading3Props) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`heading3`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
