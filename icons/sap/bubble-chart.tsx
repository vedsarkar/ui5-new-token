/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `bubble-chart` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/bubble-chart";
 * Optional component: import { BubbleChart } from "@reltio/design/icons/sap/bubble-chart";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/bubble-chart.js";

export type BubbleChartProps = Omit<IconProps, "name">;

/** Renders `<Icon name="bubble-chart" />` after this module is evaluated. */
export function BubbleChart(props: BubbleChartProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`bubble-chart`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
