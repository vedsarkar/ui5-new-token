/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `thing-type` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/thing-type";
 * Optional component: import { ThingType } from "@reltio/design/icons/sap/thing-type";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/thing-type.js";

export type ThingTypeProps = Omit<IconProps, "name">;

/** Renders `<Icon name="thing-type" />` after this module is evaluated. */
export function ThingType(props: ThingTypeProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`thing-type`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
