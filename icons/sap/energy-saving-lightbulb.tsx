/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `energy-saving-lightbulb` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/energy-saving-lightbulb";
 * Optional component: import { EnergySavingLightbulb } from "@reltio/design/icons/sap/energy-saving-lightbulb";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/energy-saving-lightbulb.js";

export type EnergySavingLightbulbProps = Omit<IconProps, "name">;

/** Renders `<Icon name="energy-saving-lightbulb" />` after this module is evaluated. */
export function EnergySavingLightbulb(props: EnergySavingLightbulbProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`energy-saving-lightbulb`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
