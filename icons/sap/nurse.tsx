/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `nurse` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/nurse";
 * Optional component: import { Nurse } from "@reltio/design/icons/sap/nurse";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/nurse.js";

export type NurseProps = Omit<IconProps, "name">;

/** Renders `<Icon name="nurse" />` after this module is evaluated. */
export function Nurse(props: NurseProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`nurse`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
