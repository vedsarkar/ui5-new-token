/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `project-definition-triangle` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/project-definition-triangle";
 * Optional component: import { ProjectDefinitionTriangle } from "@reltio/design/icons/sap/project-definition-triangle";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/project-definition-triangle.js";

export type ProjectDefinitionTriangleProps = Omit<IconProps, "name">;

/** Renders `<Icon name="project-definition-triangle" />` after this module is evaluated. */
export function ProjectDefinitionTriangle(
	props: ProjectDefinitionTriangleProps,
) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`project-definition-triangle`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
