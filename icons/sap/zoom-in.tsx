/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `zoom-in` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/zoom-in";
 * Optional component: import { ZoomIn } from "@reltio/design/icons/sap/zoom-in";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/zoom-in.js";

export type ZoomInProps = Omit<IconProps, "name">;

/** Renders `<Icon name="zoom-in" />` after this module is evaluated. */
export function ZoomIn(props: ZoomInProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`zoom-in`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
