/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `thumb-down` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/thumb-down";
 * Optional component: import { ThumbDown } from "@reltio/design/icons/sap/thumb-down";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/thumb-down.js";

export type ThumbDownProps = Omit<IconProps, "name">;

/** Renders `<Icon name="thumb-down" />` after this module is evaluated. */
export function ThumbDown(props: ThumbDownProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`thumb-down`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
