/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `work-history` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/work-history";
 * Optional component: import { WorkHistory } from "@reltio/design/icons/sap/work-history";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/work-history.js";

export type WorkHistoryProps = Omit<IconProps, "name">;

/** Renders `<Icon name="work-history" />` after this module is evaluated. */
export function WorkHistory(props: WorkHistoryProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`work-history`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
