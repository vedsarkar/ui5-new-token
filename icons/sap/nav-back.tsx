/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `nav-back` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/nav-back";
 * Optional component: import { NavBack } from "@reltio/design/icons/sap/nav-back";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/nav-back.js";

export type NavBackProps = Omit<IconProps, "name">;

/** Renders `<Icon name="nav-back" />` after this module is evaluated. */
export function NavBack(props: NavBackProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`nav-back`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
