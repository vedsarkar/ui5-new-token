/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `screen-split-two` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/screen-split-two";
 * Optional component: import { ScreenSplitTwo } from "@reltio/design/icons/sap/screen-split-two";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/screen-split-two.js";

export type ScreenSplitTwoProps = Omit<IconProps, "name">;

/** Renders `<Icon name="screen-split-two" />` after this module is evaluated. */
export function ScreenSplitTwo(props: ScreenSplitTwoProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`screen-split-two`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
