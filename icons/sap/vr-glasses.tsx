/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `vr-glasses` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/vr-glasses";
 * Optional component: import { VrGlasses } from "@reltio/design/icons/sap/vr-glasses";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/vr-glasses.js";

export type VrGlassesProps = Omit<IconProps, "name">;

/** Renders `<Icon name="vr-glasses" />` after this module is evaluated. */
export function VrGlasses(props: VrGlassesProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`vr-glasses`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
