/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `multiselect-none` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/multiselect-none";
 * Optional component: import { MultiselectNone } from "@reltio/design/icons/sap/multiselect-none";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/multiselect-none.js";

export type MultiselectNoneProps = Omit<IconProps, "name">;

/** Renders `<Icon name="multiselect-none" />` after this module is evaluated. */
export function MultiselectNone(props: MultiselectNoneProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`multiselect-none`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
