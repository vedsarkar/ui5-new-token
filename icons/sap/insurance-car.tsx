/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `insurance-car` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/insurance-car";
 * Optional component: import { InsuranceCar } from "@reltio/design/icons/sap/insurance-car";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/insurance-car.js";

export type InsuranceCarProps = Omit<IconProps, "name">;

/** Renders `<Icon name="insurance-car" />` after this module is evaluated. */
export function InsuranceCar(props: InsuranceCarProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`insurance-car`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
