/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `not-editable` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/not-editable";
 * Optional component: import { NotEditable } from "@reltio/design/icons/sap/not-editable";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/not-editable.js";

export type NotEditableProps = Omit<IconProps, "name">;

/** Renders `<Icon name="not-editable" />` after this module is evaluated. */
export function NotEditable(props: NotEditableProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`not-editable`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
