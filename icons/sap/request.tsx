/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `request` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/request";
 * Optional component: import { Request } from "@reltio/design/icons/sap/request";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/request.js";

export type RequestProps = Omit<IconProps, "name">;

/** Renders `<Icon name="request" />` after this module is evaluated. */
export function Request(props: RequestProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`request`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
