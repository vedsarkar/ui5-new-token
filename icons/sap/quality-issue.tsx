/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `quality-issue` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/quality-issue";
 * Optional component: import { QualityIssue } from "@reltio/design/icons/sap/quality-issue";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/quality-issue.js";

export type QualityIssueProps = Omit<IconProps, "name">;

/** Renders `<Icon name="quality-issue" />` after this module is evaluated. */
export function QualityIssue(props: QualityIssueProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`quality-issue`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
