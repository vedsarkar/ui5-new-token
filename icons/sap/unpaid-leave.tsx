/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `unpaid-leave` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/unpaid-leave";
 * Optional component: import { UnpaidLeave } from "@reltio/design/icons/sap/unpaid-leave";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/unpaid-leave.js";

export type UnpaidLeaveProps = Omit<IconProps, "name">;

/** Renders `<Icon name="unpaid-leave" />` after this module is evaluated. */
export function UnpaidLeave(props: UnpaidLeaveProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`unpaid-leave`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
