/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `accidental-leave` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/accidental-leave";
 * Optional component: import { AccidentalLeave } from "@reltio/design/icons/sap/accidental-leave";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/accidental-leave.js";

export type AccidentalLeaveProps = Omit<IconProps, "name">;

/** Renders `<Icon name="accidental-leave" />` after this module is evaluated. */
export function AccidentalLeave(props: AccidentalLeaveProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`accidental-leave`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
