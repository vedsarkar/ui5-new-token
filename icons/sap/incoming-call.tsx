/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `incoming-call` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/incoming-call";
 * Optional component: import { IncomingCall } from "@reltio/design/icons/sap/incoming-call";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/incoming-call.js";

export type IncomingCallProps = Omit<IconProps, "name">;

/** Renders `<Icon name="incoming-call" />` after this module is evaluated. */
export function IncomingCall(props: IncomingCallProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`incoming-call`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
