/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `learning-assistant` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/learning-assistant";
 * Optional component: import { LearningAssistant } from "@reltio/design/icons/sap/learning-assistant";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/learning-assistant.js";

export type LearningAssistantProps = Omit<IconProps, "name">;

/** Renders `<Icon name="learning-assistant" />` after this module is evaluated. */
export function LearningAssistant(props: LearningAssistantProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`learning-assistant`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
