/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `command-line-interfaces` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/command-line-interfaces";
 * Optional component: import { CommandLineInterfaces } from "@reltio/design/icons/sap/command-line-interfaces";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/command-line-interfaces.js";

export type CommandLineInterfacesProps = Omit<IconProps, "name">;

/** Renders `<Icon name="command-line-interfaces" />` after this module is evaluated. */
export function CommandLineInterfaces(props: CommandLineInterfacesProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`command-line-interfaces`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
