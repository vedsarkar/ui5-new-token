/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `time-account` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/time-account";
 * Optional component: import { TimeAccount } from "@reltio/design/icons/sap/time-account";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/time-account.js";

export type TimeAccountProps = Omit<IconProps, "name">;

/** Renders `<Icon name="time-account" />` after this module is evaluated. */
export function TimeAccount(props: TimeAccountProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`time-account`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
