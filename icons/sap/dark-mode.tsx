/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `dark-mode` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/dark-mode";
 * Optional component: import { DarkMode } from "@reltio/design/icons/sap/dark-mode";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/dark-mode.js";

export type DarkModeProps = Omit<IconProps, "name">;

/** Renders `<Icon name="dark-mode" />` after this module is evaluated. */
export function DarkMode(props: DarkModeProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`dark-mode`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
