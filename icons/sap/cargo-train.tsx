/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `cargo-train` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/cargo-train";
 * Optional component: import { CargoTrain } from "@reltio/design/icons/sap/cargo-train";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/cargo-train.js";

export type CargoTrainProps = Omit<IconProps, "name">;

/** Renders `<Icon name="cargo-train" />` after this module is evaluated. */
export function CargoTrain(props: CargoTrainProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`cargo-train`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
