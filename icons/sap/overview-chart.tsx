/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `overview-chart` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/overview-chart";
 * Optional component: import { OverviewChart } from "@reltio/design/icons/sap/overview-chart";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/overview-chart.js";

export type OverviewChartProps = Omit<IconProps, "name">;

/** Renders `<Icon name="overview-chart" />` after this module is evaluated. */
export function OverviewChart(props: OverviewChartProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`overview-chart`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
