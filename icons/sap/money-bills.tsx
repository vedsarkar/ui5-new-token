/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `money-bills` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/money-bills";
 * Optional component: import { MoneyBills } from "@reltio/design/icons/sap/money-bills";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/money-bills.js";

export type MoneyBillsProps = Omit<IconProps, "name">;

/** Renders `<Icon name="money-bills" />` after this module is evaluated. */
export function MoneyBills(props: MoneyBillsProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`money-bills`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
