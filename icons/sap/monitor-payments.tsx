/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `monitor-payments` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/monitor-payments";
 * Optional component: import { MonitorPayments } from "@reltio/design/icons/sap/monitor-payments";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/monitor-payments.js";

export type MonitorPaymentsProps = Omit<IconProps, "name">;

/** Renders `<Icon name="monitor-payments" />` after this module is evaluated. */
export function MonitorPayments(props: MonitorPaymentsProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`monitor-payments`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
