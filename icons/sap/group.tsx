/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `group` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/group";
 * Optional component: import { Group } from "@reltio/design/icons/sap/group";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/group.js";

export type GroupProps = Omit<IconProps, "name">;

/** Renders `<Icon name="group" />` after this module is evaluated. */
export function Group(props: GroupProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`group`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
