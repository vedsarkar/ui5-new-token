/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `value-help` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/value-help";
 * Optional component: import { ValueHelp } from "@reltio/design/icons/sap/value-help";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/value-help.js";

export type ValueHelpProps = Omit<IconProps, "name">;

/** Renders `<Icon name="value-help" />` after this module is evaluated. */
export function ValueHelp(props: ValueHelpProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`value-help`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
