/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `slim-arrow-left` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/slim-arrow-left";
 * Optional component: import { SlimArrowLeft } from "@reltio/design/icons/sap/slim-arrow-left";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/slim-arrow-left.js";

export type SlimArrowLeftProps = Omit<IconProps, "name">;

/** Renders `<Icon name="slim-arrow-left" />` after this module is evaluated. */
export function SlimArrowLeft(props: SlimArrowLeftProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`slim-arrow-left`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
