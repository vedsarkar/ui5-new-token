/**
 * Auto-generated by scripts/build-sap-icons.mjs — do not edit manually.
 * Run: npm run build-sap-icons
 * Source: @ui5/webcomponents-icons/dist/*.js
 * Registers the SAP Fiori `detail-less` icon and default-exports its name.
 * Icon name (default export): import name from "@reltio/design/icons/sap/detail-less";
 * Optional component: import { DetailLess } from "@reltio/design/icons/sap/detail-less";
 */

import { Icon, type IconProps } from "@reltio/design/components";
import ICON_NAME from "@ui5/webcomponents-icons/dist/detail-less.js";

export type DetailLessProps = Omit<IconProps, "name">;

/** Renders `<Icon name="detail-less" />` after this module is evaluated. */
export function DetailLess(props: DetailLessProps) {
	return <Icon name={ICON_NAME} {...props} />;
}

/** The icon's registry name (`detail-less`). Consuming this default binding pulls
 * in — and keeps — the icon-registration side effect above, so it never gets
 * tree-shaken away when the icon is actually used. */
export default ICON_NAME;
