---
"@reltio/design": minor
---

Endorse the SAP Fiori messaging, dialog, and splitter components from `@reltio/design/components` as 1:1 re-exports (doc-only directories with README, prop-type schema, and variant stories): `MessageView` (+`MessageItem`, `MessageViewButton`), `MessageBox`, `SelectDialog`, and `SplitterLayout` (+`Splitter`, `SplitterElement`). Sub-items are documented on their parent component pages.
