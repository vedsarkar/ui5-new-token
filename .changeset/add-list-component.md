---
"@reltio/design": minor
---

Add endorsed `List` surface for menus and selectable row lists: `List`, a single `ListItem` row entity (backed by SAP Fiori `ListItemStandard`, customised via props and children), and `ListItemGroup` for sectioned lists. UI5's `ListItemCustom` is intentionally not endorsed in favour of one obvious item entity.
