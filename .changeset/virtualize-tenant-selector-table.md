---
"@reltio/design": patch
---

`TenantSelector`: use SAP UI5 `TableVirtualizer` for the dialog table above 50 filtered rows. Below the threshold rendering is unchanged. Public API is untouched — consumers benefit automatically on upgrade.
