---
"@reltio/skills": patch
---

Update the `adopt-reltio-design` skill to prefer the PascalCase icon component (`<Save />`) for standalone icons, while keeping name imports for a component's `icon` prop (`<Button icon={saveIcon} />`).
