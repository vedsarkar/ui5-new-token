---
"@reltio/design": minor
---

Re-export Table selection feature components (TableSelectionMulti / TableSelectionSingle, plus TableGrowing / TableRowAction) so consumers can build selectable tables through the endorsed surface.
