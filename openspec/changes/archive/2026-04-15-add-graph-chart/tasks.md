## 1. Types

- [x] 1.1 Create `GraphChart.types.ts` with `GraphChartNode`, `GraphChartLink`, and `GraphChartProps` types

## 2. Core Implementation

- [x] 2.1 Create `GraphChart.tsx` with `buildGraphOption()` function (id-based node mapping, auto-sized nodes, force layout, hover labels, undirected edges, tooltip formatting with units)
- [x] 2.2 Create `GraphChart.module.css` with root (fill parent), overlay, and errorOverlay styles

## 3. Export

- [x] 3.1 Create `index.ts` barrel export for GraphChart component and types
- [x] 3.2 Add GraphChart re-export to `charts/index.ts`

## 4. Stories

- [x] 4.1 Create `GraphChart.stories.tsx` with stories: Default, WithLabels (link labels), WithUnits, WithNodeValues, Loading, BackgroundRefresh, Empty, Error
