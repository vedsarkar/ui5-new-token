## 1. DonutChart

- [x] 1.1 Create `charts/DonutChart/DonutChart.types.ts` with `DonutChartItem` and `DonutChartProps` types
- [x] 1.2 Create `charts/DonutChart/DonutChart.module.css` with root and overlay styles
- [x] 1.3 Create `charts/DonutChart/DonutChart.tsx` — register ECharts PieChart series, implement `buildDonutOption()` pure function, component with state management
- [x] 1.4 Create `charts/DonutChart/index.ts` with public exports
- [x] 1.5 Create `charts/DonutChart/DonutChart.stories.tsx` with required stories (Default, ManySegments, Loading, BackgroundRefresh, Empty, Error, CustomHeight)

## 2. Integration

- [x] 2.1 Update `charts/index.ts` to export DonutChart
- [x] 2.2 Run `npm run format` and `npm run lint` to verify code quality
