import type { ShellBarSpacer } from "@ui5/webcomponents-react/ShellBarSpacer";
import type { ComponentPropsWithoutRef } from "react";

/**
 * Reltio-endorsed SAP Fiori ShellBarSpacer.
 *
 * Type re-exported 1:1 from `@ui5/webcomponents-react/ShellBarSpacer` — no Reltio
 * wrapping or prop renaming. Used within `ShellBar`. See README.
 */
export type ShellBarSpacerProps = ComponentPropsWithoutRef<
	typeof ShellBarSpacer
>;
