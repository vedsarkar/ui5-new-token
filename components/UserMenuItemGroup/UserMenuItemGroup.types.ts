import type { UserMenuItemGroup } from "@ui5/webcomponents-react/UserMenuItemGroup";
import type { ComponentPropsWithoutRef } from "react";

/**
 * Reltio-endorsed SAP Fiori UserMenuItemGroup.
 *
 * Type re-exported 1:1 from `@ui5/webcomponents-react/UserMenuItemGroup` — no Reltio
 * wrapping or prop renaming. Used within `UserMenu`. See README.
 */
export type UserMenuItemGroupProps = ComponentPropsWithoutRef<
	typeof UserMenuItemGroup
>;
