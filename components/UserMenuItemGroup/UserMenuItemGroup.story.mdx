{/*
  AUTO-GENERATED — do not edit by hand.
  Sources: README.md, <Component>.types.ts, <Component>.stories.tsx
  Resolved prop types: TypeScript Compiler API walks generics + intersections + cross-package imports
  Run: npm run build-component-docs
*/}

import { Meta, Stories } from "@storybook/addon-docs/blocks";
import { JsonSchema } from "@/.storybook/blocks/JsonSchema";
import { SectionHeading } from "@/.storybook/blocks/SectionHeading";
import * as UserMenuItemGroupStories from "./UserMenuItemGroup.stories";
import schema from "./UserMenuItemGroup.schema.json";

<Meta of={UserMenuItemGroupStories} />

# UserMenuItemGroup

```tsx
import { UserMenuItemGroup } from "@reltio/design/components";
```

`UserMenuItemGroup` groups items inside a `UserMenu`.

`UserMenuItemGroup` is re-exported 1:1 from `@ui5/webcomponents-react/UserMenuItemGroup`; its props, slots, and behavior are exactly the UI5 component's. The Reltio layer adds curation (the endorsed surface), pinned versioning, and documentation.

### Usage

Use within a `UserMenu` to group related account/menu items.

### See also

- [UI5 React documentation](https://ui5.github.io/webcomponents-react/v2/?path=/docs/modals-popovers-usermenu--docs)
- `UserMenu` — the parent component

<SectionHeading>Prop types</SectionHeading>

<JsonSchema schema={schema} />

<div style={{ marginTop: 32 }}>
	<Stories />
</div>
