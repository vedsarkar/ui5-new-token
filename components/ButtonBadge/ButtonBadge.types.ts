import type { ButtonBadge } from "@ui5/webcomponents-react/ButtonBadge";
import type { ComponentPropsWithoutRef } from "react";

/**
 * Reltio-endorsed SAP Fiori ButtonBadge.
 *
 * Type re-exported 1:1 from `@ui5/webcomponents-react/ButtonBadge` — no Reltio
 * wrapping or prop renaming. Used within `Button`. See README.
 */
export type ButtonBadgeProps = ComponentPropsWithoutRef<typeof ButtonBadge>;
