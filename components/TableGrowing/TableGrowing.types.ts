import type { TableGrowing } from "@ui5/webcomponents-react/TableGrowing";
import type { ComponentPropsWithoutRef } from "react";

/**
 * Reltio-endorsed SAP Fiori TableGrowing.
 *
 * Type re-exported 1:1 from `@ui5/webcomponents-react/TableGrowing` — no Reltio
 * wrapping or prop renaming. Used within `Table`. See README.
 */
export type TableGrowingProps = ComponentPropsWithoutRef<typeof TableGrowing>;
