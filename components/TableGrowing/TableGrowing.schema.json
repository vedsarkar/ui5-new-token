{
	"$schema": "https://json-schema.org/draft/2020-12/schema",
	"title": "TableGrowingProps",
	"x-component": "TableGrowing",
	"type": "object",
	"properties": {
		"className": {
			"description": "CSS Class Name which will be appended to the most outer element of a component.\nUse this prop carefully, overwriting CSS rules might break the component.",
			"x-typescriptType": "string",
			"type": "string"
		},
		"mode": {
			"description": "Defines the mode of the <code>ui5-table</code> growing.\n\nAvailable options are:\n\nButton - Shows a More button at the bottom of the table, pressing it will load more rows.\n\nScroll - The rows are loaded automatically by scrolling to the bottom of the table. If the table is not scrollable,\na growing button will be rendered instead to ensure growing functionality.",
			"default": "Button",
			"x-typescriptType": "'Button' | 'Scroll' | TableGrowingMode"
		},
		"onLoadMore": {
			"description": "Fired when the growing button is pressed or the user scrolls to the end of the table.\n\n| cancelable | bubbles |\n| :--------: | :-----: |\n| ❌|✅|",
			"x-typescriptType": "(event: Ui5CustomEvent<TableGrowingDomRef>) => void",
			"x-functionSignature": "(event: Ui5CustomEvent<TableGrowingDomRef>) => void"
		},
		"style": {
			"description": "Element style which will be appended to the most outer element of a component.\nUse this prop carefully, some css properties might break the component.",
			"x-typescriptType": "CSSProperties"
		},
		"subtext": {
			"description": "Defines the text that will be displayed below the `text` inside the growing button.\nHas no effect when mode is set to Scroll.",
			"x-typescriptType": "string",
			"type": "string"
		},
		"text": {
			"description": "Defines the text that will be displayed inside the growing button.\nHas no effect when mode is set to `Scroll`.\n\n**Note:** When not provided and the mode is set to Button, a default text is displayed, corresponding to the\ncurrent language.",
			"x-typescriptType": "string",
			"type": "string"
		},
		"waitForDefine": {
			"description": "Defines whether the component should wait for the underlying custom element of the web component to be defined. This can be useful, for example, for using instance methods when mounting the component.\n\n__Note:__ This adds a rendering cycle to your component.",
			"x-typescriptType": "boolean",
			"type": "boolean"
		}
	}
}
