import type { ToolbarSelect } from "@ui5/webcomponents-react/ToolbarSelect";
import type { ComponentPropsWithoutRef } from "react";

/**
 * Reltio-endorsed SAP Fiori ToolbarSelect.
 *
 * Type re-exported 1:1 from `@ui5/webcomponents-react/ToolbarSelect` — no Reltio
 * wrapping or prop renaming. Used within `Toolbar`. See README.
 */
export type ToolbarSelectProps = ComponentPropsWithoutRef<typeof ToolbarSelect>;
