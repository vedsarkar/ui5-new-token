import type { SpecialCalendarDate } from "@ui5/webcomponents-react/SpecialCalendarDate";
import type { ComponentPropsWithoutRef } from "react";

/**
 * Reltio-endorsed SAP Fiori SpecialCalendarDate.
 *
 * Type re-exported 1:1 from `@ui5/webcomponents-react/SpecialCalendarDate` — no Reltio
 * wrapping or prop renaming. Used within `Calendar`. See README.
 */
export type SpecialCalendarDateProps = ComponentPropsWithoutRef<
	typeof SpecialCalendarDate
>;
