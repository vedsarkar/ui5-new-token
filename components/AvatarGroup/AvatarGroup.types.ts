import type { AvatarGroup } from "@ui5/webcomponents-react/AvatarGroup";
import type { ComponentPropsWithoutRef } from "react";

/**
 * Reltio-endorsed SAP Fiori AvatarGroup.
 *
 * Type re-exported 1:1 from `@ui5/webcomponents-react/AvatarGroup` — no Reltio
 * wrapping or prop renaming. Used within `Avatar`. See README.
 */
export type AvatarGroupProps = ComponentPropsWithoutRef<typeof AvatarGroup>;
