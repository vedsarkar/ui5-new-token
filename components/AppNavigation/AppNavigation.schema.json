{
	"$schema": "https://json-schema.org/draft/2020-12/schema",
	"title": "AppNavigationProps",
	"x-component": "AppNavigation",
	"type": "object",
	"properties": {
		"apps": {
			"description": "Application catalog grouped by category, as returned by the Reltio\nConfig Service. Only `name` and `url` are read from each app; the icon\nis resolved internally from the app name. Groups and apps missing\nrequired fields are skipped.",
			"x-typescriptType": "Array<AppNavigationGroup>",
			"type": "array"
		},
		"accessibleName": {
			"description": "Accessible ARIA name announced for the navigation landmark\n(e.g. \"Applications\"). Forwarded to the underlying `SideNavigation`.",
			"default": "Applications",
			"x-typescriptType": "string",
			"type": "string"
		},
		"collapsable": {
			"description": "Render a collapse / expand toggle pinned at the bottom of the menu,\nswitching it to an icon-only rail.",
			"default": false,
			"x-typescriptType": "boolean",
			"type": "boolean"
		},
		"env": {
			"description": "Environment identifier substituted into URL templates (`${environment}`).",
			"x-typescriptType": "string",
			"type": "string"
		},
		"homeUrl": {
			"description": "URL of the application's home page. When provided, a \"Home\" entry with\nthe SAP `home` icon is rendered as the first item, above the app groups.\nSupports `${environment}` / `${tenant}` placeholders.",
			"x-typescriptType": "string",
			"type": "string"
		},
		"tenant": {
			"description": "Tenant identifier substituted into URL templates (`${tenant}`).",
			"x-typescriptType": "string",
			"type": "string"
		}
	},
	"required": ["apps"]
}
