import type { TableSelectionMulti } from "@ui5/webcomponents-react/TableSelectionMulti";
import type { ComponentPropsWithoutRef } from "react";

/**
 * Reltio-endorsed SAP Fiori TableSelectionMulti.
 *
 * Type re-exported 1:1 from `@ui5/webcomponents-react/TableSelectionMulti` — no Reltio
 * wrapping or prop renaming. Used within `Table`. See README.
 */
export type TableSelectionMultiProps = ComponentPropsWithoutRef<
	typeof TableSelectionMulti
>;
