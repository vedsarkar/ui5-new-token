import type { CalendarLegendItem } from "@ui5/webcomponents-react/CalendarLegendItem";
import type { ComponentPropsWithoutRef } from "react";

/**
 * Reltio-endorsed SAP Fiori CalendarLegendItem.
 *
 * Type re-exported 1:1 from `@ui5/webcomponents-react/CalendarLegendItem` — no Reltio
 * wrapping or prop renaming. Used within `Calendar`. See README.
 */
export type CalendarLegendItemProps = ComponentPropsWithoutRef<
	typeof CalendarLegendItem
>;
