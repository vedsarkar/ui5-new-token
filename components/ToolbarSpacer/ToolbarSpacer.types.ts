import type { ToolbarSpacer } from "@ui5/webcomponents-react/ToolbarSpacer";
import type { ComponentPropsWithoutRef } from "react";

/**
 * Reltio-endorsed SAP Fiori ToolbarSpacer.
 *
 * Type re-exported 1:1 from `@ui5/webcomponents-react/ToolbarSpacer` — no Reltio
 * wrapping or prop renaming. Used within `Toolbar`. See README.
 */
export type ToolbarSpacerProps = ComponentPropsWithoutRef<typeof ToolbarSpacer>;
