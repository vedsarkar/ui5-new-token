import type { MenuItemGroup } from "@ui5/webcomponents-react/MenuItemGroup";
import type { ComponentPropsWithoutRef } from "react";

/**
 * Reltio-endorsed SAP Fiori MenuItemGroup.
 *
 * Type re-exported 1:1 from `@ui5/webcomponents-react/MenuItemGroup` — no Reltio
 * wrapping or prop renaming. Used within `Menu`. See README.
 */
export type MenuItemGroupProps = ComponentPropsWithoutRef<typeof MenuItemGroup>;
