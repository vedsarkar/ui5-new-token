{/*
  AUTO-GENERATED — do not edit by hand.
  Sources: README.md, <Component>.types.ts, <Component>.stories.tsx
  Resolved prop types: TypeScript Compiler API walks generics + intersections + cross-package imports
  Run: npm run build-component-docs
*/}

import { Meta, Stories } from "@storybook/addon-docs/blocks";
import { JsonSchema } from "@/.storybook/blocks/JsonSchema";
import { SectionHeading } from "@/.storybook/blocks/SectionHeading";
import * as UploadCollectionItemStories from "./UploadCollectionItem.stories";
import schema from "./UploadCollectionItem.schema.json";

<Meta of={UploadCollectionItemStories} />

# UploadCollectionItem

```tsx
import { UploadCollectionItem } from "@reltio/design/components";
```

`UploadCollectionItem` is one file row in an `UploadCollection`.

`UploadCollectionItem` is re-exported 1:1 from `@ui5/webcomponents-react/UploadCollectionItem`; its props, slots, and behavior are exactly the UI5 component's. The Reltio layer adds curation (the endorsed surface), pinned versioning, and documentation.

### Usage

Use as children of `UploadCollection`; set `fileName` and body metadata.

### See also

- [UI5 React documentation](https://ui5.github.io/webcomponents-react/v2/?path=/docs/inputs-uploadcollection--docs)
- `UploadCollection` — the parent component

<SectionHeading>Prop types</SectionHeading>

<JsonSchema schema={schema} />

<div style={{ marginTop: 32 }}>
	<Stories />
</div>
