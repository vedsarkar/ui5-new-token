import type { AvatarBadge } from "@ui5/webcomponents-react/AvatarBadge";
import type { ComponentPropsWithoutRef } from "react";

/**
 * Reltio-endorsed SAP Fiori AvatarBadge.
 *
 * Type re-exported 1:1 from `@ui5/webcomponents-react/AvatarBadge` — no Reltio
 * wrapping or prop renaming. Used within `Avatar`. See README.
 */
export type AvatarBadgeProps = ComponentPropsWithoutRef<typeof AvatarBadge>;
