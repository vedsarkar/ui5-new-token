import type { TabContainer } from "@ui5/webcomponents-react/TabContainer";
import type { ComponentPropsWithoutRef } from "react";

/**
 * Reltio-endorsed SAP Fiori TabContainer.
 *
 * Type re-exported 1:1 from `@ui5/webcomponents-react/TabContainer` — no
 * Reltio wrapping or prop renaming. Pair with `Tab` from `@reltio/design`
 * for tab items.
 */
export type TabContainerProps = ComponentPropsWithoutRef<typeof TabContainer>;
