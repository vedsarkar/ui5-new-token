{
	"$schema": "https://json-schema.org/draft/2020-12/schema",
	"$id": "https://reltio.design/apps/catalog.schema.json",
	"title": "Reltio Apps Catalog",
	"description": "Catalog of Reltio platform applications with metadata for navigation, integration, and automation.",
	"type": "object",
	"required": ["apps"],
	"additionalProperties": false,
	"properties": {
		"$schema": {
			"type": "string",
			"description": "Reference to the JSON Schema for validation."
		},
		"apps": {
			"type": "array",
			"description": "List of available applications.",
			"items": {
				"type": "object",
				"required": [
					"id",
					"name",
					"description",
					"basePath",
					"icon",
					"category"
				],
				"additionalProperties": false,
				"properties": {
					"id": {
						"type": "string",
						"pattern": "^[a-z0-9]+(-[a-z0-9]+)*$",
						"description": "Unique stable identifier in kebab-case. Used as a key for integrations and automation."
					},
					"name": {
						"type": "string",
						"minLength": 1,
						"description": "Human-readable display name of the application."
					},
					"description": {
						"type": "string",
						"minLength": 1,
						"description": "Brief description of the application's purpose and capabilities."
					},
					"basePath": {
						"type": "string",
						"pattern": "^/",
						"description": "Base path for the application in the Next.js multi-zones architecture."
					},
					"icon": {
						"type": "string",
						"format": "uri",
						"description": "Absolute URL to the application icon (SVG)."
					},
					"category": {
						"type": "string",
						"enum": [
							"AI",
							"Applications",
							"Configuration",
							"Resources",
							"Security",
							"Tenant Management"
						],
						"description": "Application category for grouping in navigation."
					}
				}
			}
		}
	}
}
