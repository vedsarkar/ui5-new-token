import type { Tab } from "@ui5/webcomponents-react/Tab";
import type { ComponentPropsWithoutRef } from "react";

/**
 * Reltio-endorsed SAP Fiori Tab.
 *
 * Type re-exported 1:1 from `@ui5/webcomponents-react/Tab` — no Reltio
 * wrapping or prop renaming. Used within `TabContainer`. See README.
 */
export type TabProps = ComponentPropsWithoutRef<typeof Tab>;
