{/*
  AUTO-GENERATED — do not edit by hand.
  Sources: README.md, <Component>.types.ts, <Component>.stories.tsx
  Resolved prop types: TypeScript Compiler API walks generics + intersections + cross-package imports
  Run: npm run build-component-docs
*/}

import { Meta, Stories } from "@storybook/addon-docs/blocks";
import { JsonSchema } from "@/.storybook/blocks/JsonSchema";
import { SectionHeading } from "@/.storybook/blocks/SectionHeading";
import * as TableSelectionSingleStories from "./TableSelectionSingle.stories";
import schema from "./TableSelectionSingle.schema.json";

<Meta of={TableSelectionSingleStories} />

# TableSelectionSingle

```tsx
import { TableSelectionSingle } from "@reltio/design/components";
```

`TableSelectionSingle` enables single-row selection in a `Table`.

`TableSelectionSingle` is re-exported 1:1 from `@ui5/webcomponents-react/TableSelectionSingle`; its props, slots, and behavior are exactly the UI5 component's. The Reltio layer adds curation (the endorsed surface), pinned versioning, and documentation.

### Usage

Place in the `features` slot of a `Table`.

### See also

- [UI5 React documentation](https://ui5.github.io/webcomponents-react/v2/?path=/docs/data-display-table--docs)
- `Table` — the parent component

<SectionHeading>Prop types</SectionHeading>

<JsonSchema schema={schema} />

<div style={{ marginTop: 32 }}>
	<Stories />
</div>
