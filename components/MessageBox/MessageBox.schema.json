{
	"$schema": "https://json-schema.org/draft/2020-12/schema",
	"title": "MessageBoxProps",
	"x-component": "MessageBox",
	"type": "object",
	"properties": {
		"children": {
			"description": "Defines the content of the `MessageBox`.\n\n**Note:** Although this prop accepts HTML Elements, it is strongly recommended that you only use text in order to preserve the intended design and accessibility capabilities.",
			"x-typescriptType": "ReactNode | Array<ReactNode>"
		},
		"accessibleDescription": {
			"description": "Defines the accessible description of the component.\n\n**Note:** Available since [v2.11.0](https://github.com/UI5/webcomponents/releases/tag/v2.11.0) of **@ui5/webcomponents**.",
			"x-typescriptType": "string",
			"type": "string"
		},
		"accessibleDescriptionRef": {
			"description": "Receives id(or many ids) of the elements that describe the component.\n\n**Note:** Available since [v2.11.0](https://github.com/UI5/webcomponents/releases/tag/v2.11.0) of **@ui5/webcomponents**.",
			"x-typescriptType": "string",
			"type": "string"
		},
		"accessibleName": {
			"description": "Defines the accessible name of the component.",
			"x-typescriptType": "string",
			"type": "string"
		},
		"accessibleNameRef": {
			"description": "Defines the IDs of the elements that label the component.\n\n__Note:__ Per default the prop receives the IDs of the header and the content.",
			"x-typescriptType": "string",
			"type": "string"
		},
		"accessibleRole": {
			"description": "Allows setting a custom role.\n\n**Note:** Available since [v1.10.0](https://github.com/UI5/webcomponents/releases/tag/v1.10.0) of **@ui5/webcomponents**.",
			"default": "Dialog",
			"x-typescriptType": "PopupAccessibleRole | 'None' | 'Dialog' | 'AlertDialog'"
		},
		"actions": {
			"description": "Array of actions of the MessageBox. Those actions will be transformed into buttons in the `MessageBox` footer.\n\n__Note:__ Although this prop accepts all HTML Elements, it is strongly recommended that you only use `MessageBoxAction`s (text) or the `Button` component in order to preserve the intended.",
			"x-typescriptType": "Array<ReactNode>",
			"type": "array"
		},
		"className": {
			"description": "CSS Class Name which will be appended to the most outer element of a component.\nUse this prop carefully, overwriting CSS rules might break the component.",
			"x-typescriptType": "string",
			"type": "string"
		},
		"draggable": {
			"description": "Determines whether the component is draggable.\nIf this property is set to true, the Dialog will be draggable by its header.\n\n**Note:** The component can be draggable only in desktop mode.\n\n**Note:** This property overrides the default HTML \"draggable\" attribute native behavior.\nWhen \"draggable\" is set to true, the native browser \"draggable\"\nbehavior is prevented and only the Dialog custom logic (\"draggable by its header\") works.",
			"default": false,
			"x-typescriptType": "boolean",
			"type": "boolean"
		},
		"emphasizedAction": {
			"description": "Specifies which action of the created dialog will be emphasized.",
			"default": "`\"OK\"`",
			"x-since": "0.16.3",
			"x-typescriptType": "string",
			"type": "string"
		},
		"header": {
			"description": "Defines the header HTML Element.\n\n**Note:** If `header` slot is provided, the labelling of the dialog is a responsibility of the application developer.\n`accessibleName` should be used.\n\n__Note:__ The content of the prop will be rendered into a [&lt;slot&gt;](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/slot) by assigning the respective [slot](https://developer.mozilla.org/en-US/docs/Web/HTML/Global_attributes/slot) attribute (`slot=\"header\"`).\nSince you can't change the DOM order of slots when declaring them within a prop, it might prove beneficial to manually mount them as part of the component's children, especially when facing problems with the reading order of screen readers.\n\n__Note:__ When passing a custom React component to this prop, you have to make sure your component reads the `slot` prop and appends it to the most outer element of your component.\nLearn more about it [here](https://ui5.github.io/webcomponents-react/v2/?path=/docs/knowledge-base-handling-slots--docs).\n\n__Supported Node Type/s:__ `Array<HTMLElement>`",
			"x-typescriptType": "false | ReactElement<any, string | JSXElementConstructor<any>> | Iterable<ReducedReactNode> | ReactElement<any, string | JSXElementConstructor<any>> | Array<InternalUI5WCSlotsNode>"
		},
		"icon": {
			"description": "A custom icon. If not present, it will be derived from the `MessageBox` type.",
			"x-typescriptType": "string | number | boolean | ReactElement<any, string | JSXElementConstructor<any>> | Iterable<ReactNode> | ReactPortal"
		},
		"initialFocus": {
			"description": "Defines the ID of the HTML Element or the `MessageBoxAction`, which will get the initial focus.",
			"x-typescriptType": "string",
			"type": "string"
		},
		"onBeforeClose": {
			"description": "Fired before the component is closed. This event can be cancelled, which will prevent the popup from closing.\n\n**Note:** Call `event.preventDefault()` inside the handler of this event to prevent its default action/s.\n\n| cancelable | bubbles |\n| :--------: | :-----: |\n| ✅|❌|",
			"x-typescriptType": "(event: Ui5CustomEvent<DialogDomRef, PopupBeforeCloseEventDetail>) => void",
			"x-functionSignature": "(event: Ui5CustomEvent<DialogDomRef, PopupBeforeCloseEventDetail>) => void"
		},
		"onBeforeOpen": {
			"description": "Fired before the component is opened. This event can be cancelled, which will prevent the popup from opening.\n\n**Note:** Call `event.preventDefault()` inside the handler of this event to prevent its default action/s.\n\n| cancelable | bubbles |\n| :--------: | :-----: |\n| ✅|❌|",
			"x-typescriptType": "(event: Ui5CustomEvent<DialogDomRef>) => void",
			"x-functionSignature": "(event: Ui5CustomEvent<DialogDomRef>) => void"
		},
		"onClose": {
			"description": "Callback to be executed when the `MessageBox` is closed (either by pressing on one of the `actions` or by pressing the Escape key).\n`action` is the pressed action button, it's `undefined` when closed via ESC.",
			"x-typescriptType": "(action: string | undefined, escPressed?: true) => void",
			"x-functionSignature": "(action: string | undefined, escPressed?: true) => void"
		},
		"onOpen": {
			"description": "Fired after the component is opened.\n\n| cancelable | bubbles |\n| :--------: | :-----: |\n| ❌|❌|",
			"x-typescriptType": "(event: Ui5CustomEvent<DialogDomRef>) => void",
			"x-functionSignature": "(event: Ui5CustomEvent<DialogDomRef>) => void"
		},
		"open": {
			"description": "Flag whether the Message Box should be opened or closed",
			"x-typescriptType": "boolean",
			"type": "boolean"
		},
		"preventFocusRestore": {
			"description": "Defines if the focus should be returned to the previously focused element,\nwhen the popup closes.",
			"default": false,
			"x-typescriptType": "boolean",
			"type": "boolean"
		},
		"preventInitialFocus": {
			"description": "Indicates whether initial focus should be prevented.\n\n**Note:** Available since [v2.0.0](https://github.com/UI5/webcomponents/releases/tag/v2.0.0) of **@ui5/webcomponents**.",
			"default": false,
			"x-typescriptType": "boolean",
			"type": "boolean"
		},
		"resizable": {
			"description": "Configures the component to be resizable.\nIf this property is set to true, the Dialog will have a resize handle in its bottom right corner in LTR languages.\nIn RTL languages, the resize handle will be placed in the bottom left corner.\n\n**Note:** The component can be resizable only in desktop mode.\n\n**Note:** Upon resizing, externally defined height and width styling will be ignored.",
			"default": false,
			"x-typescriptType": "boolean",
			"type": "boolean"
		},
		"stretch": {
			"description": "Determines if the dialog will be stretched to full screen on mobile. On desktop,\nthe dialog will be stretched to approximately 90% of the viewport.\n\n**Note:** For better usability of the component it is recommended to set this property to \"true\" when the dialog is opened on phone.",
			"default": false,
			"x-typescriptType": "boolean",
			"type": "boolean"
		},
		"style": {
			"description": "Element style which will be appended to the most outer element of a component.\nUse this prop carefully, some css properties might break the component.",
			"x-typescriptType": "CSSProperties"
		},
		"titleText": {
			"description": "A custom title for the MessageBox. If not present, it will be derived from the `MessageBox` type.",
			"x-typescriptType": "string",
			"type": "string"
		},
		"type": {
			"description": "Defines the type of the `MessageBox` with predefined title, icon, actions and a visual highlight color.",
			"default": "`\"Confirm\"`",
			"x-typescriptType": "'Information' | MessageBoxType | 'Confirm' | 'Error' | 'Success' | 'Warning'"
		}
	},
	"required": ["children"]
}
