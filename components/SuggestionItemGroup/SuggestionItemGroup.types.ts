import type { SuggestionItemGroup } from "@ui5/webcomponents-react/SuggestionItemGroup";
import type { ComponentPropsWithoutRef } from "react";

/**
 * Reltio-endorsed SAP Fiori SuggestionItemGroup.
 *
 * Type re-exported 1:1 from `@ui5/webcomponents-react/SuggestionItemGroup` — no Reltio
 * wrapping or prop renaming. Used within `Input`. See README.
 */
export type SuggestionItemGroupProps = ComponentPropsWithoutRef<
	typeof SuggestionItemGroup
>;
