import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Guides/Typography" />

# Typography

`@reltio/design` ships **SAP 72** as the default text font and **72 Mono** as the default monospace font. This guide shows how to load them in your application and how to use them when writing your own CSS on top of the design system.

The same `@font-face` rules feed both **Reltio components** and **UI5 web components** from `@ui5/webcomponents-react` — UI5's default `--sapFontFamily` token already resolves to `"72"`, so once the fonts are loaded UI5 picks them up automatically.

> **🎯 Key Principle:** Once `fonts.css` is loaded, every UI5 and Reltio component renders in SAP 72. Your own elements inherit the same family through standard CSS inheritance — you almost never need to declare `font-family` yourself.

## Quick Start

Load the platform's static CSS files in `<head>` and set the `data-theme` attribute on any ancestor element:

```html
<link rel="stylesheet" href="https://reltio.design/variables.css" />
<link rel="stylesheet" href="https://reltio.design/fonts.css" />

<div data-theme="horizon-light">
  <!-- Your app — UI5 + Reltio components both use SAP 72 -->
</div>
```

`variables.css` declares all `--sap*` design tokens (including `--sapFontFamily`); `fonts.css` registers the SAP 72 `@font-face` rules and points the document root at the SAP 72 stack. Toggle the active theme by setting `data-theme="horizon-light"` or `data-theme="horizon-dark"` on any ancestor element.

Where do these files come from? `variables.css` is generated by `npm run build-tokens` from the repo-local token files (`utils/sap_horizon.tokens.json`, `utils/sap_horizon_dark.tokens.json`), which mirror SAP Horizon's [`@sap-theming/theming-base-content`](https://github.com/SAP/theming-base-content) key surface 1:1 with Reltio-customised values. `fonts.css` is generated separately by `npm run build-fonts`. The SAP 72 `.woff2` binaries are vendored directly in `public/fonts/`. The output (`public/variables.css`, `public/fonts.css`, `public/fonts/*.woff2`) is committed to the repo and published at `https://reltio.design/`.

> **💡 Self-hosting fonts?** Copy the `.woff2` binaries to your own static hosting and serve a `fonts.css` from the same origin so that the `url(...)` paths inside resolve correctly. For air-gapped/CSP-locked environments, you can also download the `.woff2` files directly from SAP ([Fiori Design Web — Font 72 downloads](https://www.sap.com/design-system/fiori-design-web/resources/downloads#font-72)).

## Available Faces

The default text family is `"72"`. The default monospace family is `"72 Mono"`.

<table>
	<thead>
		<tr>
			<th>Family</th>
			<th>Weight</th>
			<th>Style</th>
			<th>Typical use</th>
		</tr>
	</thead>
	<tbody>
		<tr>
			<td><code>"72"</code></td>
			<td>300 (Light)</td>
			<td>normal</td>
			<td>Display headings, hero text</td>
		</tr>
		<tr>
			<td><code>"72"</code></td>
			<td>400 (Regular)</td>
			<td>normal</td>
			<td>Body text (default)</td>
		</tr>
		<tr>
			<td><code>"72"</code></td>
			<td>400</td>
			<td>italic</td>
			<td>Emphasis, captions</td>
		</tr>
		<tr>
			<td><code>"72"</code></td>
			<td>600 (Semibold)</td>
			<td>normal</td>
			<td>Buttons, labels, table headers</td>
		</tr>
		<tr>
			<td><code>"72"</code></td>
			<td>700 (Bold)</td>
			<td>normal</td>
			<td>Headings, banner titles</td>
		</tr>
		<tr>
			<td><code>"72"</code></td>
			<td>700</td>
			<td>italic</td>
			<td>Bold emphasis</td>
		</tr>
		<tr>
			<td><code>"72"</code></td>
			<td>900 (Black)</td>
			<td>normal</td>
			<td>Major page titles in the SAP Fiori hero style</td>
		</tr>
		<tr>
			<td><code>"72-SemiboldDuplex"</code></td>
			<td>400</td>
			<td>normal</td>
			<td>Interactive-affordance text where width must stay stable across weights</td>
		</tr>
		<tr>
			<td><code>"72 Mono"</code></td>
			<td>400 (Regular)</td>
			<td>normal</td>
			<td>Code blocks, JSON, log output</td>
		</tr>
		<tr>
			<td><code>"72 Mono"</code></td>
			<td>700 (Bold)</td>
			<td>normal</td>
			<td>Emphasized code, status badges</td>
		</tr>
	</tbody>
</table>

> **Need another weight?** SAP also distributes Condensed and CondensedBold. They are not bundled by default — reach out to the UI Center of Excellence team if your design requires them.

## Using the Fonts in Your CSS

### Default text — just inherit

The platform relies on CSS inheritance for `font-family`. When you build your own components or page sections on top of `@reltio/design`, do not redeclare the family — your elements pick up SAP 72 automatically from `:root`.

**✅ Do:** Style other properties, leave `font-family` alone

```css
.title {
	font-size: 14px;
	font-weight: 600;
	color: var(--sapTextColor);
}
```

**❌ Don't:** Re-declare a different family

```css
.title {
	font-family: "Roboto", sans-serif; /* breaks visual consistency */
	font-size: 14px;
}
```

### Form controls — use `font-family: inherit`

Browser user agents reset `font-family` on `<input>`, `<textarea>`, `<select>`, and `<button>` elements. To make them inherit from the page, set it back explicitly:

```css
.input {
	font-family: inherit;
	font-size: 14px;
}
```

This is the only place where declaring `font-family` in your component CSS is expected.

### Monospace and code — use the 72 Mono stack

For code blocks, JSON viewers, log output, or any monospace context, use the canonical stack:

```css
.codeBlock {
	font-family: "72 Mono", ui-monospace, "SF Mono", Menlo, Consolas, monospace;
}
```

The fallbacks ensure graceful degradation if 72 Mono fails to load:

- `ui-monospace` — modern OS monospace.
- `"SF Mono"`, `Menlo`, `Consolas` — platform-specific defaults.
- `monospace` — generic family as last resort.

## How Character Coverage Works

Each weight is registered twice in `fonts.css` with different `unicode-range` values:

<table>
	<thead>
		<tr>
			<th>Subset</th>
			<th>Range</th>
			<th>When the browser fetches it</th>
		</tr>
	</thead>
	<tbody>
		<tr>
			<td>Latin</td>
			<td><code>U+0000–00FF</code> + selected Latin punctuation</td>
			<td>Always — typical Western European UI uses only this</td>
		</tr>
		<tr>
			<td>Full Unicode</td>
			<td><code>U+0100–FFFF</code></td>
			<td>Only when a non-Latin character (Cyrillic, Greek, CJK, Arabic, ...) appears on the page</td>
		</tr>
	</tbody>
</table>

You don't need to do anything — the browser picks the right file per character automatically. This keeps the default page weight low (around 50 KB per loaded weight) while still covering international content correctly when needed.

## Why SAP 72

SAP 72 is the native typeface of SAP Fiori. Reltio Design uses SAP Fiori as its primary visual reference, so adopting 72 keeps Reltio surfaces visually consistent with adjacent SAP UI5 web components and the broader enterprise data ecosystem. Practical benefits:

- **Predictable rendering** across operating systems — no more "the text looks slightly different on macOS vs Windows".
- **Smooth visual blending** when Reltio components are embedded next to SAP UI5 web components.
- **Self-hosted, no third-party CDN** — fonts are served from `reltio.design` (or your own origin) with long cache headers.

## Quick Reference

<table>
	<thead>
		<tr>
			<th>I want to...</th>
			<th>Do this</th>
		</tr>
	</thead>
	<tbody>
		<tr>
			<td>Set up SAP 72 in my app</td>
			<td>
				<code>{`<link rel="stylesheet" href="https://reltio.design/fonts.css" />`}</code>
				(or <code>{`import "@reltio/design/fonts.css"`}</code>) + set <code>:root</code> font-family
			</td>
		</tr>
		<tr>
			<td>Use the default font in my own component</td>
			<td>Nothing — inherit from <code>:root</code></td>
		</tr>
		<tr>
			<td>Style a form control</td>
			<td>Add <code>font-family: inherit</code> to the input/textarea</td>
		</tr>
		<tr>
			<td>Render code or JSON</td>
			<td>Use <code>"72 Mono", ui-monospace, "SF Mono", Menlo, Consolas, monospace</code></td>
		</tr>
		<tr>
			<td>Render Cyrillic, Greek, CJK, Arabic</td>
			<td>Nothing — the browser pulls the extended subset automatically</td>
		</tr>
	</tbody>
</table>

## Resources

- [SAP 72 typeface overview](https://experience.sap.com/fiori-design-web/typography/) — official SAP Fiori typography guidance
- [MDN: `@font-face`](https://developer.mozilla.org/en-US/docs/Web/CSS/@font-face) — CSS spec for self-hosted fonts
- [MDN: `unicode-range`](https://developer.mozilla.org/en-US/docs/Web/CSS/@font-face/unicode-range) — per-subset on-demand downloads
